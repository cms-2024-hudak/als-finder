#!/usr/bin/env bash
set -e

# Install als-finder in editable mode using pre-installed conda packages
pip install --no-build-isolation --no-deps -e .

# Register the R kernel so Jupyter can execute R notebooks natively
R -e "IRkernel::installspec(user = FALSE)" || true
